"""Automated validation pipeline for LLM-generated Python micro-agents.

Walking skeleton: proves the generate -> validate -> decide -> register pipe
end-to-end with a real static-analysis layer (L1) and structured stubs for
policy (L2), sandbox (L3), and LLM judge (L4).
"""

__version__ = "0.1.0"
